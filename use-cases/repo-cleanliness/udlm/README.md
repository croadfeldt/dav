# Repo cleanliness — UDLM scoping set

The nine repo-cleanliness UCs (single-source, consistency, standards-documented, data-pipelines-complete, boundary-respected, provider-neutral, correctly-scoped-docs, referential-integrity, versioning-discipline) scoped to the **udlm** repo. Run against udlm to gate documentation & model quality over time. Uuids `uc-clean-udlm-00N`, handles `repo-clean/udlm/<slug>`, `profile: udlm`. Sibling set: the other repo under `../`.

Profile vocab in `consumer-profile.yaml`.
