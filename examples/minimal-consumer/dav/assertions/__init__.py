# Assertion modules for the TinyURL minimal-consumer example.
#
# These modules are imported by DAV when it loads this consumer's use cases.
# The module path `minimal_consumer.assertions.<name>` is relative to the
# consumer-content root as configured in stage-config/stages.yaml.
