uuid: uc-canonical-002-fsi
handle: test/cross-profile/tenant-boundary-enforcement-fsi
scenario:
  description: "[profile=fsi] Validates GRP-001 (every resource must belong to exactly one tenant_boundary group) and the\
    \ profile-governed enforcement model. In minimal profile, tenancy is advisory \u2014 the request succeeds with a warning.\
    \ In standard+ profiles, tenancy is mandatory \u2014 a request without a valid tenant_uuid is rejected by the GateKeeper.\
    \ Cross-profile test validates P1: security property (tenancy) is PRESENT in all profiles; only enforcement strictness\
    \ varies."
  actor:
    persona: consumer
    profile: fsi
  intent: Validates GRP-001 (every resource must belong to exactly one tenant_boundary group) and the profile-governed enforcement
    model.
  success_criteria:
  - GRP-001 (every resource must belong to exactly one tenant_boundary group) and the profile-governed enforcement model
  - 'validates P1: security property (tenancy) is PRESENT in all profiles; only enforcement strictness varies'
  dimensions:
    lifecycle_phase: new_request
    resource_complexity: single_no_deps
    policy_complexity: single_gatekeeper
    provider_landscape: single_eligible
    governance_context: standard_governance
    failure_mode: happy_path
  profile: fsi
  expected_domain_interactions:
  - domain: 15-universal-groups
    interaction: "Section 10.3 \u2014 tenant_boundary"
  - domain: 15-universal-groups
    interaction: "Section 10.3 \u2014 profile-governed enforcement"
  - domain: 00-design-priorities
    interaction: "Priority 1 \u2014 Security"
generated_by:
  mode: regression
  source: human-authored
tags: []
metadata: {}
