uc: must-realize/fx-ref-credential-realize2
expected_verdict: supported
expected_gaps: []
must_not_report:
- capability_id: FIX-ROTATE-001
  why: 'CONTROL. 30-credentials.md specifies reference-based resolution at realization time and
    65-rotation.md fully specifies the rotate operation — atomic replacement, stable identifier.
    Every consumer in this scenario is realized after the rotation, so the unspecified refresh of
    pre-rotation consumers is not exercised; reporting it here is a false positive.'
