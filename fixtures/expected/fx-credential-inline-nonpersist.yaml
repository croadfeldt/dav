uc: must-reject/fx-credential-inline-nonpersist
expected_verdict: partially_supported
expected_gaps:
- capability_id: FIX-NONPERSIST-001
  why: 30-credentials.md 'Handling of a rejected body' is empty. The refusal is specified but non-retention
    of the transmitted secret is not — a conforming implementation could refuse the intent and still
    persist the secret in a request log.
must_not_report:
- capability_id: FIX-TYPED-001
  why: CREDENTIAL_INLINE is in the closed code set and remediation names credref://. The refusal
    itself is fully specified.
