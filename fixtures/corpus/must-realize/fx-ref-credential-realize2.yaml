uuid: uc-fx00000021
handle: must-realize/fx-ref-credential-realize2
version: 1.0.0
scenario:
  description: 'A tenant''s intent references a secret by credential reference,
    credref://tenant-a/api-key, per 30-credentials.md — no literal secret appears in the intent
    body, and Halyard resolves the reference at realization time against the tenant''s secret
    store. The credential''s owner later issues rotate per 65-rotation.md: the material is
    atomically replaced and the reference identifier is unchanged. A further resource is then
    realized against the same reference and resolves the post-rotation material. Every consumer in
    this scenario is realized AFTER the rotation — none holds pre-rotation material. This case is
    fully specified and is present as a CONTROL: a correct analysis should report no gap here.'
  actor:
    persona: application-team-member
    profile: standard
  intent: Realize resources against a credential reference, rotate the credential, and realize
    again against the stable reference
  success_criteria:
  - The intent carries only the credential reference; resolution happens at realization time
    against the tenant's secret store
  - rotate atomically replaces the secret material and the reference identifier is stable across
    the rotation
  - A resource realized after the rotation resolves the post-rotation material
  dimensions:
    lifecycle_phase: new_request
    resource_complexity: single_no_deps
    policy_complexity: single_validation
    provider_landscape: single_eligible
    governance_context: standard_governance
    failure_mode: happy_path
  profile: standard
  perspectives:
  - security-officer
generated_by:
  mode: authoring
  source: human-authored
  model: claude
  prompt_version: fixture-2026-07-27
  timestamp: '2026-07-30T00:00:00.000000+00:00'
success_semantics: realize
tags:
- fixture
- must-realize
- credentials
- rotation
- control
