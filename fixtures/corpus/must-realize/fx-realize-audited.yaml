uuid: uc-fx00000022
handle: must-realize/fx-realize-audited
version: 1.0.0
scenario:
  description: 'A tenant realizes a single resource, batch-runner, and the compliance auditor
    verifies the operation''s trace afterwards. Per 80-audit.md, the realize operation appends an
    audit record naming the caller identity, the tenant, the resource, the operation, and the
    outcome; records are append-only and carry a monotonic per-tenant sequence number, retained for
    the tenant''s configured retention window. No projection read occurs in this scenario and no
    integrity-proof claim is made — the scored surface is the realization audit trail itself. This
    case is fully specified and is present as a CONTROL: a correct analysis should report no gap
    here.'
  actor:
    persona: application-team-member
    profile: standard
  intent: Realize a resource and have the operation leave a complete, ordered audit record the
    auditor can verify
  success_criteria:
  - The realize operation appends an audit record naming the caller identity, the tenant, the
    resource, the operation, and the outcome
  - Audit records are append-only and carry a monotonic per-tenant sequence number
  - The auditor can reconstruct the tenant's operation history from the records within the
    configured retention window
  dimensions:
    lifecycle_phase: new_request
    resource_complexity: single_no_deps
    policy_complexity: single_validation
    provider_landscape: single_eligible
    governance_context: standard_governance
    failure_mode: happy_path
  profile: standard
  perspectives:
  - compliance-auditor
generated_by:
  mode: authoring
  source: human-authored
  model: claude
  prompt_version: fixture-2026-07-27
  timestamp: '2026-07-30T00:00:00.000000+00:00'
success_semantics: realize
tags:
- fixture
- must-realize
- audit
- control
