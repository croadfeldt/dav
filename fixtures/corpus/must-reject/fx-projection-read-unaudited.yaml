uuid: uc-fx00000024
handle: must-reject/fx-projection-read-unaudited
version: 1.0.0
scenario:
  description: Tenant A exposes a masked projection of a resource to tenant B — the sanctioned
    cross-tenant read surface. A member of tenant B reads it. The read is legal and succeeds;
    the guarantee under test is the refusal-adjacent one that makes the boundary auditable — the
    access must leave a record. Every realize, update, and teardown is audited; if reads of
    masked projections are not, cross-tenant data access is the one boundary crossing that
    leaves no trace.
  actor:
    persona: application-team-member
    profile: standard
  intent: Read another tenant's masked projection and have the access appear in the audit
    record, retrievable by the owning tenant's auditor
  success_criteria:
  - The read of the masked projection succeeds; projections are the cross-tenant read surface (20-tenancy.md)
  - The read appends an audit record naming the caller identity, both tenants, the resource,
    and the operation, matching the fields the realization audit requires (80-audit.md)
  - The record is append-only and carries the per-tenant monotonic sequence number like
    operation records (80-audit.md)
  - Tenant A's auditor can retrieve the record within the configured retention window
  dimensions:
    lifecycle_phase: new_request
    resource_complexity: single_no_deps
    policy_complexity: cross_domain_constraint
    provider_landscape: single_eligible
    governance_context: standard_governance
    failure_mode: happy_path
  profile: standard
  perspectives:
  - compliance-auditor
  - security-officer
generated_by:
  mode: authoring
  source: human-authored
  model: claude
  prompt_version: fixture-2026-07-27
  timestamp: '2026-07-30T00:00:00.000000+00:00'
success_semantics: refuse
tags:
- fixture
- must-reject
- auditable
- projection
