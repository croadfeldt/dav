uuid: uc-fx00000027
handle: must-reject/fx-federation-anonymous-refused
version: 1.0.0
scenario:
  description: A registered peer instance dispatches an intent that arrives without an
    originating caller identity — only the instance identity is present. Success is the
    REFUSAL. The receiving instance must refuse with FEDERATION_ANONYMOUS before any tenancy
    or policy evaluation runs, because the only identity it could evaluate against is the
    instance's own, and the on-behalf-of model forbids exactly that substitution.
  actor:
    persona: federation-peer-operator
    profile: standard
  intent: Dispatch an intent to a peer without the originating caller's identity attached, and
    have it refused rather than evaluated as the instance
  success_criteria:
  - The dispatched intent arriving without an originating caller identity is refused (70-federation.md)
  - The refusal carries reason_code FEDERATION_ANONYMOUS
  - No tenancy or policy evaluation proceeds against the instance identity in place of a caller
  dimensions:
    lifecycle_phase: new_request
    resource_complexity: single_no_deps
    policy_complexity: single_validation
    provider_landscape: single_eligible
    governance_context: standard_governance
    failure_mode: policy_violation
  profile: standard
  perspectives:
  - security-officer
generated_by:
  mode: authoring
  source: human-authored
  model: claude
  prompt_version: fixture-2026-07-27
  timestamp: '2026-07-30T00:00:00.000000+00:00'
success_semantics: refuse
tags:
- fixture
- must-reject
- federation
- control
